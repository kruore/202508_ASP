
-----




io 모델에서 고성능 서버로 들어가기 위해서는 IOCP를 사용할 수밖에 없다.

SELECT 모델
Overlapped 구조


IOCP 구조

SELECT 모델의 문제점 : 

블로킹 소켓이라면?

데이터를 하나 처리하기까지 다음 데이터를 받지 못한다.
RECV에서. 즉 스레드가 1만개가 생성된다.


1만의 유저가 동시 접속한다며면.

 **컨텍스트 스위칭(Context Switching)**과 **[[스레싱(Thrashing)]]** 


단순히 스레드를 멈추고 다시 실행하는 것 같지만, 실제로는 CPU 레지스터 상태를 저장/복구하고, 무엇보다 **CPU 캐시(Cache)가 다 날아가버리는(Cache Pollution)** 비용이 엄청남.



[[블로킹, 논블로킹 소캣]]
